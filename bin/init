#!/bin/bash -e

# Start dbus daemon (removing any lingering pid file if restarting)
rm -fr /var/run/dbus/pid
dbus-daemon --system

# Start avahi daemon
avahi-daemon -D

# Start homebridge
exec node_modules/.bin/homebridge
